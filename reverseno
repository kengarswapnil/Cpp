#include<iostream>

using namespace std;

int main(){

    int sum=0;
    int no=123;

    while(no>0){
        int rem=no%10;
        sum=sum*10+rem;
        no=no/10;
    }
    cout <<sum;
    
    return 0;
}
